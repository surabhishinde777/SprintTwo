# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SprintTwo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire'

  # Pods for SprintTwo
  
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'

  target 'SprintTwoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SprintTwoUITests' do
    # Pods for testing
  end

end
